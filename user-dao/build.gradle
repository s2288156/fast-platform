dependencies {
    compile project(':tool-lib')
    compile 'org.springframework.boot:spring-boot-starter-data-jpa'
    runtimeOnly 'mysql:mysql-connector-java'
}

test {
    useJUnitPlatform()
}
jar.enabled=true
bootJar {
    enabled false
}