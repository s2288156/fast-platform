spring:
  application:
    name: user
  datasource:
    url: jdbc:mysql://org.mysql.com:3306/db_user?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        enable_lazy_load_no_trans: true
  lifecycle:
    timeout-per-shutdown-phase: 15s # kill程序后15s后再停止程序，此过程中不会接收新的请求
  jackson:
    default-property-inclusion: non_null
server:
  port: 18000
  servlet:
    context-path: /user
  shutdown: graceful # graceful优雅的关机， immediate 立即停止
